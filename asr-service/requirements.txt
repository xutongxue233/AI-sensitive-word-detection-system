# asr-service: faster-whisper(CTranslate2)轻量 CPU 语音识别依赖。
# 不含 torch、不依赖 CUDA,适配只有核显的机器;CPU 上比 openai-whisper 约快 4 倍、更省内存。
# faster-whisper 会自动拉入 ctranslate2 / tokenizers / onnxruntime / av / huggingface-hub。
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.27
faster-whisper==1.1.0
opencc-python-reimplemented==0.1.7
