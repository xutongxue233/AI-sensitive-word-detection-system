# ocr-service: 画面硬字幕 OCR(RapidOCR / onnxruntime)纯 CPU 轻量依赖。
# 模型(PP-OCRv4 中英文检测/识别)随 rapidocr-onnxruntime wheel 自带,离线可用、免下载;
# 不含 paddlepaddle/torch/CUDA,适配只有核显的机器。
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.27
rapidocr-onnxruntime==1.3.24
opencc-python-reimplemented==0.1.7
numpy==1.26.4
