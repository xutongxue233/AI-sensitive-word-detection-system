# ocr-service: 画面硬字幕 OCR(RapidOCR / onnxruntime)轻量依赖。
# 模型(PP-OCRv4 中英文检测/识别)随 rapidocr-onnxruntime wheel 自带,离线可用、免下载;
# 不含 paddlepaddle/torch/CUDA。
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.27
rapidocr-onnxruntime==1.3.24
opencc-python-reimplemented==0.1.7
numpy==1.26.4
# 核显加速(推荐): onnxruntime-directml 提供 DmlExecutionProvider(AMD/Intel/NVIDIA 的 DX12 GPU
# 含核显通吃)。它与 rapidocr 依赖的 CPU 版 onnxruntime 是同名 import 的两个发行包、不能共存,
# 因此不能写进本文件一次性安装;须在装完本文件后换装(start-local.bat setup 已自动执行):
#   pip uninstall -y onnxruntime && pip install onnxruntime-directml==1.23.0
# 不换装则保持纯 CPU 推理,功能与接口完全不变;OCR_USE_DML 环境变量可强制开关(默认 auto)。
